.env
__pycache__/
*.pyc
node_modules/
dist/
.DS_Store
*.log

# Claude Code: personal/per-machine config and audit logs
.claude/settings.local.json
.claude/remote-ops.log
# Codex (OpenAI agent) local config — not part of the repo
.codex/
# uv lock files: not currently committed (Dockerfiles fall back to `uv sync`).
# Future PR may track these for reproducible builds.
uv.lock
